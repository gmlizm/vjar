package com.aboo.vjar.util.javassist;

public class MainTest {

	/*public static void main(String[] args) throws Exception {
		ProxyFactory proxyFactory = new ProxyFactory();
		proxyFactory.setSuperclass(Foo.class);
		proxyFactory.writeDirectory=".";
		Proxy proxy = (Proxy) proxyFactory.create(new Class[]{}, new Object[]{});
		proxy.setHandler(new XMethodHandler());
		Foo foo = (Foo) proxy;
		foo.test();
		foo.testfinal();
		
//		ClassPool pool = ClassPool.getDefault();
//		CtClass cc = pool.get(proxy.getClass().getName());

	}
*/
}
